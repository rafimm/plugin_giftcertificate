<button type="button" class="remove-btn-lg btn btn-light"
        data-target=".cart.cart-page"
        data-name="${lineItem.lineItemText}"
        data-action="${pdict.actionUrls.removeGiftCertificate}"
        data-uuid="${lineItem.UUID}"
        aria-label="${Resource.msgf('text.remove.line.item','cart', null, lineItem.lineItemText)}">
    <span aria-hidden="true">&times;</span>
</button>
